# ResuForge AI - 开发需求文档

> **文档版本**：v1.0  
> **创建日期**：2026-09-11  
> **项目定位**：AI 驱动的交互式简历生成器系统（前端求职作品集 Demo）  
> **核心目标**：展示全栈 + AI 工程化能力，兼顾面试亮点与实际可用性

---

## 📋 目录

1. [项目概述](#项目概述)
2. [技术栈确认](#技术栈确认)
3. [核心功能需求](#核心功能需求)
4. [架构设计](#架构设计)
5. [开发阶段规划](#开发阶段规划)
6. [Git 版本管理规范](#git-版本管理规范)
7. [开发约束与原则](#开发约束与原则)
8. [环境变量配置](#环境变量配置)
9. [面试亮点提炼](#面试亮点提炼)

---

## 📌 项目概述

### 项目背景
用户正在准备前端求职，需要一个能展示技术能力的 Demo 项目。该项目需要：
- 体现前端 + AI + 后端的全栈能力
- 具备实际可用性（不是纯 Mock）
- 技术栈符合 2026 年行业主流趋势
- 开发周期可控（3-4 周）

### 核心价值主张
为求职者提供 AI 驱动的简历内容生成与优化，包括：
- 职位描述智能改写
- 技能关键词推荐
- 项目经历优化建议
- 简历排版与 PDF 导出

---

## 🛠 技术栈确认

### 前端技术栈
| 技术 | 选型 | 理由 |
|------|------|------|
| **框架** | Next.js 15 (App Router) | 现代化全栈框架，支持 SSR/SSG，Vercel 原生支持 |
| **语言** | TypeScript | 类型安全，面试加分 |
| **状态管理** | Zustand | 轻量、开发效率高，适合中等复杂度项目 |
| **UI 组件库** | shadcn/ui | 现代化、可定制、展示技术前瞻性 |
| **样式方案** | Tailwind CSS | 快速开发，配合 shadcn/ui 使用 |
| **表单管理** | React Hook Form | 性能好、验证简单 |
| **PDF 生成** | react-pdf 或 jsPDF | 前端直接生成，无需后端 |

### 后端技术栈
| 技术 | 选型 | 理由 |
|------|------|------|
| **API 层** | Next.js API Routes (App Router) | 前后端一体，无需独立后端服务 |
| **认证方案** | JWT (jose 库) | 无状态认证，支持多用户数据隔离 |
| **数据库** | Supabase PostgreSQL | BaaS 方案，提供数据库 + Row Level Security |
| **ORM/SDK** | Supabase Client SDK | 简化数据库操作，自动处理用户上下文 |

### AI 技术栈
| 技术 | 选型 | 理由 |
|------|------|------|
| **LLM 提供商** | DeepSeek | 用户已有 API Key，国内访问稳定 |
| **AI 编排框架** | LangChain.js | 展示 AI 工程化能力，管理 Prompt 和调用链 |
| **调用方式** | Vercel Serverless Functions | 后端调用，保护 API Key 安全 |

### 部署方案
- **托管平台**：Vercel（前端 + Serverless Functions）
- **数据库**：Supabase Cloud（免费层）
- **域名**：（用户后续配置）

---

## 🎯 核心功能需求

### MVP 功能范围

#### 1. 用户认证系统
**需求描述**：
- **预设 3 个演示账号**（Supabase `users` 表初始化数据）：
  - `user1@demo.com` / `demo123` / userId: `user-001`
  - `user2@demo.com` / `demo123` / userId: `user-002`
  - `user3@demo.com` / `demo123` / userId: `user-003`
- 使用 JWT 实现登录认证
- 每个用户数据完全隔离（Supabase Row Level Security）

**技术实现**：
- JWT Token 存储在 httpOnly Cookie 中（防 XSS）
- Token 有效期 24 小时
- 后端中间件自动验证 Token 并提取 userId
- 密码使用 bcrypt 加密存储（即使是演示账号）

**验收标准**：
- ✅ 可以用任意演示账号登录
- ✅ 登录后跳转到仪表盘
- ✅ Token 过期后跳转登录页
- ✅ 不同账号数据互不干扰
- ✅ 错误的密码无法登录

#### 2. 简历信息管理
**需求描述**：
- 用户可以创建/编辑/删除多份简历
- 每份简历包含以下模块：
  - 个人信息（姓名、联系方式、求职意向）
  - 教育经历
  - 工作经历
  - 项目经历
  - 技能列表
  - 自我评价

**技术实现**：
- 使用 React Hook Form 管理表单
- 数据存储在 Supabase 的 `resumes` 表
- 使用 Zustand 管理当前编辑的简历状态
- 自动保存草稿（防止数据丢失）

**验收标准**：
- ✅ 可以创建新简历
- ✅ 表单实时验证（必填项、格式校验）
- ✅ 数据保存到数据库
- ✅ 可以查看简历列表并选择编辑

#### 3. AI 内容生成
**需求描述**：
- 用户输入基础信息后，AI 可以生成/优化以下内容：
  - **职位描述改写**：输入岗位职责，AI 改写成简历格式
  - **项目经历优化**：输入项目描述，AI 提炼亮点和量化数据
  - **技能关键词推荐**：基于目标岗位推荐相关技能
  - **自我评价生成**：基于简历内容生成个性化自我评价

**技术实现**：
- 使用 LangChain.js 管理不同场景的 Prompt 模板
- 通过 Next.js API Route 调用 DeepSeek API
- 支持流式输出（实时显示生成内容）
- 用户可以接受/拒绝/修改 AI 生成的内容

**验收标准**：
- ✅ 点击"AI 优化"按钮后显示加载状态
- ✅ AI 生成的内容逐字显示（流式输出）
- ✅ 用户可以再次生成（重新调用 AI）
- ✅ 生成失败时显示友好错误提示

#### 4. 简历预览与导出
**需求描述**：
- 提供多种简历模板（至少 2 种）
- 实时预览简历排版效果
- 支持导出为 PDF 文件

**技术实现**：
- 使用 Tailwind CSS 设计响应式简历模板
- 使用 react-pdf 或 jsPDF 生成 PDF
- 提供"打印样式"优化

**验收标准**：
- ✅ 可以切换不同模板
- ✅ 预览内容与输入内容实时同步
- ✅ 导出的 PDF 格式美观，适合打印
- ✅ PDF 文件名包含用户姓名和日期

#### 5. 数据管理
**需求描述**：
- 简历列表展示（卡片形式）
- 支持删除简历（需二次确认）
- 支持导出简历数据为 JSON（备份）

**技术实现**：
- 使用 shadcn/ui 的 Dialog 组件实现确认弹窗
- JSON 导出使用 `Blob` + `download` 属性

**验收标准**：
- ✅ 简历列表按创建时间倒序排列
- ✅ 删除前需要确认弹窗
- ✅ 可以下载 JSON 备份文件

---

## 🏗 架构设计

### 项目目录结构

```
resuforge-ai/
├── app/                          # Next.js App Router
│   ├── (auth)/                   # 认证相关页面分组
│   │   ├── login/                # 登录页
│   │   └── layout.tsx            # 认证布局（居中卡片）
│   ├── (dashboard)/              # 仪表盘页面分组
│   │   ├── dashboard/            # 简历列表页
│   │   ├── resume/               # 简历编辑页
│   │   │   ├── [id]/             # 动态路由：编辑已有简历
│   │   │   └── new/              # 创建新简历
│   │   └── layout.tsx            # Dashboard 布局（侧边栏导航）
│   ├── api/                      # API Routes
│   │   ├── auth/                 # 认证相关 API
│   │   │   ├── login/route.ts
│   │   │   └── logout/route.ts
│   │   ├── resumes/              # 简历 CRUD API
│   │   │   ├── route.ts          # GET 列表 / POST 创建
│   │   │   └── [id]/route.ts     # GET / PUT / DELETE 单个简历
│   │   └── ai/                   # AI 功能 API
│   │       ├── optimize/route.ts # 内容优化
│   │       └── generate/route.ts # 内容生成
│   ├── layout.tsx                # 全局布局
│   └── page.tsx                  # 首页（重定向到 /login）
├── components/                   # React 组件
│   ├── ui/                       # shadcn/ui 组件
│   ├── resume/                   # 简历相关组件
│   │   ├── ResumeForm.tsx        # 简历表单
│   │   ├── ResumePreview.tsx     # 简历预览
│   │   └── TemplateSelector.tsx  # 模板选择器
│   └── shared/                   # 共享组件
│       ├── Navbar.tsx
│       └── ProtectedRoute.tsx
├── lib/                          # 工具库
│   ├── supabase.ts               # Supabase 客户端配置
│   ├── auth.ts                   # JWT 认证工具
│   ├── langchain/                # LangChain 配置
│   │   ├── chains.ts             # AI 调用链
│   │   └── prompts.ts            # Prompt 模板
│   └── utils.ts                  # 通用工具函数
├── store/                        # Zustand 状态管理
│   ├── authStore.ts              # 用户认证状态
│   └── resumeStore.ts            # 简历编辑状态
├── types/                        # TypeScript 类型定义
│   ├── resume.ts
│   └── api.ts
├── public/                       # 静态资源
└── .env.local                    # 环境变量（不提交到 Git）
```

### 数据库设计（Supabase）

#### users 表
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);

-- 插入 3 个演示账号
INSERT INTO users (email, password_hash) VALUES
  ('user1@demo.com', '$2b$10$hashed_password_1'),
  ('user2@demo.com', '$2b$10$hashed_password_2'),
  ('user3@demo.com', '$2b$10$hashed_password_3');
```

#### resumes 表
```sql
CREATE TABLE resumes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  title VARCHAR(255) NOT NULL,
  content JSONB NOT NULL,  -- 存储完整简历数据
  template VARCHAR(50) DEFAULT 'classic',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 启用 Row Level Security
ALTER TABLE resumes ENABLE ROW LEVEL SECURITY;

-- 用户只能访问自己的简历
CREATE POLICY "Users can only access their own resumes"
  ON resumes
  FOR ALL
  USING (auth.uid() = user_id);
```

### API 设计

#### 认证 API
```
POST /api/auth/login
Body: { email: string, password: string }
Response: { success: boolean, token?: string }

POST /api/auth/logout
Response: { success: boolean }
```

#### 简历 API
```
GET /api/resumes
Response: { data: Resume[] }

POST /api/resumes
Body: { title: string, content: ResumeContent }
Response: { data: Resume }

GET /api/resumes/[id]
Response: { data: Resume }

PUT /api/resumes/[id]
Body: { title?: string, content?: ResumeContent }
Response: { data: Resume }

DELETE /api/resumes/[id]
Response: { success: boolean }
```

#### AI API
```
POST /api/ai/optimize
Body: { type: 'job_description' | 'project', content: string }
Response: { optimized: string }

POST /api/ai/generate
Body: { type: 'skills' | 'summary', context: ResumeContent }
Response: { generated: string }
```

---

## 📅 开发阶段规划

### 总体原则
- **每个阶段完成后必须验证功能可用**
- **阶段间不留半成品代码**
- **每个阶段结束时提交 Git**
- **重大技术决策前必须与用户确认**

---

### 第 1 阶段：项目初始化与基础架构（1-2 天）

#### 目标
搭建完整的开发环境，确保基础设施可运行。

#### 任务清单
1. **初始化 Next.js 项目**
   ```bash
   npx create-next-app@latest resuforge-ai --typescript --tailwind --app
   ```
   - 选择 App Router
   - 启用 TypeScript
   - 配置 Tailwind CSS

2. **安装核心依赖**
   ```bash
   # UI 组件库
   npx shadcn-ui@latest init
   
   # 状态管理
   npm install zustand
   
   # 表单管理
   npm install react-hook-form zod @hookform/resolvers
   
   # 认证
   npm install jose bcryptjs
   
   # 数据库
   npm install @supabase/supabase-js
   
   # AI 框架
   npm install langchain @langchain/community
   ```

3. **配置环境变量**
   - 创建 `.env.local` 文件
   - 配置 Supabase 连接
   - 配置 DeepSeek API Key
   - 配置 JWT Secret

4. **搭建项目目录结构**
   - 按照架构设计创建文件夹
   - 创建基础的 `layout.tsx` 文件

5. **Supabase 数据库初始化**
   - 创建 `users` 表和 `resumes` 表
   - 插入 3 个演示账号数据
   - 配置 Row Level Security 策略

#### 验收标准
- ✅ `npm run dev` 可以正常启动项目
- ✅ 访问 `http://localhost:3000` 显示默认首页
- ✅ 环境变量配置正确（不报错）
- ✅ Supabase 数据库连接成功（测试查询）

#### Git 提交
```bash
git add .
git commit -m "chore: 初始化项目和基础架构"
```

---

### 第 2 阶段：用户认证系统（2-3 天）

#### 目标
实现完整的登录/登出功能，确保 JWT 认证和用户数据隔离。

#### 任务清单
1. **创建登录页 UI**
   - 使用 shadcn/ui 的 Card 和 Form 组件
   - 实现邮箱和密码输入框
   - 添加表单验证（React Hook Form + Zod）

2. **实现登录 API**
   - `POST /api/auth/login`
   - 验证用户邮箱和密码
   - 生成 JWT Token
   - 设置 httpOnly Cookie

3. **实现登出 API**
   - `POST /api/auth/logout`
   - 清除 Cookie

4. **创建认证中间件**
   - 验证 JWT Token
   - 提取 userId
   - 保护需要认证的 API 和页面

5. **创建认证状态管理**
   - 使用 Zustand 管理登录状态
   - 实现 `useAuth` Hook

6. **实现 ProtectedRoute 组件**
   - 未登录自动跳转到登录页
   - 登录后跳转到仪表盘

#### 验收标准
- ✅ 可以用 3 个演示账号登录
- ✅ 错误的密码无法登录
- ✅ 登录后跳转到 `/dashboard`
- ✅ 未登录访问 `/dashboard` 会跳转到 `/login`
- ✅ 登出后 Token 失效

#### Git 提交
```bash
git add .
git commit -m "feat: 实现用户认证系统（登录/登出/JWT）"
```

---

### 第 3 阶段：简历 CRUD 功能（3-4 天）

#### 目标
实现简历的创建、查看、编辑、删除功能。

#### 任务清单
1. **创建简历列表页**
   - 显示当前用户的所有简历（卡片形式）
   - 添加"新建简历"按钮
   - 实现删除简历功能（二次确认）

2. **实现简历 API**
   - `GET /api/resumes` - 获取列表
   - `POST /api/resumes` - 创建简历
   - `GET /api/resumes/[id]` - 获取单个简历
   - `PUT /api/resumes/[id]` - 更新简历
   - `DELETE /api/resumes/[id]` - 删除简历

3. **创建简历编辑表单**
   - 个人信息模块
   - 教育经历模块（支持多项）
   - 工作经历模块（支持多项）
   - 项目经历模块（支持多项）
   - 技能列表模块
   - 自我评价模块

4. **实现表单验证**
   - 使用 Zod 定义验证规则
   - 必填项检查
   - 格式验证（邮箱、手机号等）

5. **实现自动保存功能**
   - 使用 `useDebounce` Hook
   - 每 30 秒自动保存草稿

6. **创建简历状态管理**
   - 使用 Zustand 管理当前编辑的简历

#### 验收标准
- ✅ 可以创建新简历
- ✅ 表单数据保存到数据库
- ✅ 可以编辑已有简历
- ✅ 可以删除简历（有确认弹窗）
- ✅ 不同用户的简历数据完全隔离
- ✅ 自动保存功能正常工作

#### Git 提交
```bash
git add .
git commit -m "feat: 实现简历 CRUD 功能"
```

---

### 第 4 阶段：AI 内容生成（4-5 天）

#### 目标
接入 DeepSeek API，实现 AI 驱动的内容优化功能。

#### 目标
这是项目的核心亮点，需要重点打磨。

#### 任务清单
1. **配置 LangChain**
   - 初始化 DeepSeek 模型
   - 创建 Prompt 模板库
   - 实现调用链管理

2. **实现职位描述优化 API**
   - `POST /api/ai/optimize`
   - 类型：`job_description`
   - 输入：岗位职责原文
   - 输出：简历格式的工作内容

3. **实现项目经历优化 API**
   - 类型：`project`
   - 输入：项目描述
   - 输出：突出亮点和量化数据

4. **实现技能推荐 API**
   - `POST /api/ai/generate`
   - 类型：`skills`
   - 输入：目标岗位 + 现有经历
   - 输出：推荐的技能关键词列表

5. **实现自我评价生成 API**
   - 类型：`summary`
   - 输入：完整简历数据
   - 输出：个性化自我评价

6. **前端集成 AI 功能**
   - 在表单中添加"AI 优化"按钮
   - 实现流式输出显示（逐字显示）
   - 添加加载状态和错误处理
   - 支持重新生成

7. **优化 Prompt 质量**
   - 测试不同 Prompt 的效果
   - 调整温度参数和 Token 限制

#### 验收标准
- ✅ AI 优化按钮正常工作
- ✅ 生成内容质量符合预期
- ✅ 流式输出显示流畅
- ✅ 错误时显示友好提示
- ✅ API Key 不会暴露到前端

#### Git 提交
```bash
git add .
git commit -m "feat: 接入 DeepSeek AI 实现内容生成与优化"
```

---

### 第 5 阶段：简历预览与导出（2-3 天）

#### 目标
提供美观的简历模板和 PDF 导出功能。

#### 任务清单
1. **设计简历模板**
   - 经典模板（Classic）
   - 现代模板（Modern）
   - 使用 Tailwind CSS 实现响应式布局

2. **实现实时预览**
   - 创建 `ResumePreview` 组件
   - 左右分栏布局（编辑 | 预览）
   - 实时同步数据

3. **实现模板切换**
   - 创建 `TemplateSelector` 组件
   - 保存用户选择的模板

4. **集成 PDF 生成**
   - 选择 react-pdf 或 jsPDF
   - 实现"导出 PDF"按钮
   - 优化打印样式
   - 文件名格式：`{姓名}_简历_{日期}.pdf`

5. **优化导出质量**
   - 确保字体正常显示
   - 调整页边距和行距
   - 测试多页简历的分页效果

#### 验收标准
- ✅ 可以切换 2 种模板
- ✅ 预览内容与输入实时同步
- ✅ 导出的 PDF 格式美观
- ✅ PDF 文件名正确
- ✅ 支持打印预览

#### Git 提交
```bash
git add .
git commit -m "feat: 实现简历预览和 PDF 导出功能"
```

---

### 第 6 阶段：完善与测试（2-3 天）

#### 目标
修复 Bug，优化用户体验，准备部署。

#### 任务清单
1. **功能测试**
   - 使用 3 个演示账号测试所有流程
   - 记录并修复发现的 Bug

2. **错误处理优化**
   - 统一错误提示样式
   - 添加网络请求失败重试机制
   - 优化加载状态显示

3. **性能优化**
   - 使用 Next.js Image 组件优化图片
   - 代码分割（动态导入）
   - 优化首屏加载速度

4. **无障碍优化**
   - 添加 ARIA 标签
   - 确保键盘导航可用

5. **编写 README**
   - 项目介绍
   - 技术栈说明
   - 本地开发指南
   - 部署说明

6. **部署到 Vercel**
   - 配置环境变量
   - 测试生产环境功能
   - 检查 API 调用是否正常

#### 验收标准
- ✅ 所有功能正常工作
- ✅ 无明显 Bug
- ✅ 页面加载速度 < 3 秒
- ✅ README 文档完整
- ✅ 生产环境部署成功

#### Git 提交
```bash
git add .
git commit -m "chore: 完善功能、优化性能并部署到生产环境"
```

---

## 🔧 Git 版本管理规范

### 分支策略
```
main          ← 稳定版本（每个阶段完成后合并）
 ↑
dev           ← 开发分支（日常开发）
 ↑
feature/*     ← 功能分支（可选，复杂功能使用）
```

### 提交规范
遵循 [Conventional Commits](https://www.conventionalcommits.org/)：

```
feat: 新功能
fix: Bug 修复
docs: 文档更新
style: 代码格式调整（不影响功能）
refactor: 重构（不增加功能，不修复 Bug）
test: 测试相关
chore: 构建/工具配置
```

### 提交时机
| 场景 | 操作 |
|------|------|
| **功能模块完成** | 立即提交（如完成登录 API） |
| **阶段性成果** | 提交并打 Tag（如 `v0.1-auth`） |
| **临时实验** | 使用单独分支，成功后合并或删除 |
| **重大 Bug 修复** | 单独提交，注明 `fix:` |

### 回滚机制
| 问题类型 | 回滚方式 |
|---------|---------|
| **代码错误但未提交** | `git restore <file>` 恢复文件 |
| **已提交但未推送** | `git reset --soft HEAD~1` 撤销提交 |
| **已推送到远程** | `git revert <commit>` 创建反向提交 |
| **整个阶段失败** | `git reset --hard <上一个稳定提交>` |

### 关键原则
1. **每个阶段结束后必须提交**
2. **提交前确保代码可运行**
3. **不提交 `.env.local` 等敏感文件**
4. **使用 `.gitignore` 排除 `node_modules/` 和 `.next/`**

---

## ⚠️ 开发约束与原则

### 代码修改约束
1. **最小改动原则**
   - 只修改必要的代码，不进行无关重构
   - 一次只解决一个问题
   - 避免"顺手"修改其他代码

2. **准确性优先原则**
   - 修改前必须完整阅读相关代码
   - 理解代码意图后再修改
   - 不确定的地方先询问用户

3. **功能完整性原则**
   - 一个功能要么全做完，要么不做
   - 不留半成品代码在主分支
   - 未完成的功能使用 `feature/` 分支

### 重大决策确认机制
以下情况**必须先询问用户**：

| 决策类型 | 示例 |
|---------|------|
| **技术方案变更** | "是否将 react-pdf 改为 jsPDF？" |
| **依赖包替换** | "是否将 Zustand 改为 Redux？" |
| **数据库结构变更** | "是否需要添加 `avatar_url` 字段？" |
| **API 接口调整** | "是否将 `/api/ai/optimize` 拆分为两个接口？" |
| **删除已有功能** | "是否删除自动保存功能？" |

### AI 开发协作流程
1. **接到任务后先澄清需求**
   - 复述理解："你的意思是...对吗？"
   - 确认边界："这个功能是否需要包含...？"

2. **开发前说明计划**
   - "我准备这样实现：[步骤 1、2、3]"
   - "预计会修改以下文件：[文件列表]"

3. **遇到问题及时沟通**
   - "原方案遇到问题：[具体问题]"
   - "建议改为：[新方案]，是否同意？"

4. **完成后总结变更**
   - "已完成 [功能名称]"
   - "修改了 [文件列表]"
   - "下一步建议：[后续任务]"

---

## 🔐 环境变量配置

创建 `.env.local` 文件：

```env
# Supabase 配置
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# DeepSeek API
DEEPSEEK_API_KEY=your-deepseek-api-key
DEEPSEEK_BASE_URL=https://api.deepseek.com

# JWT 配置
JWT_SECRET=your-random-secret-string-here

# 环境
NODE_ENV=development
```

### 安全注意事项
1. **绝不提交 `.env.local` 到 Git**
2. **前端只使用 `NEXT_PUBLIC_` 开头的变量**
3. **API Key 必须在后端调用**

---

## 🎯 面试亮点提炼

### 技术亮点（简历上怎么写）
1. **AI 工程化能力**
   > 使用 LangChain.js 构建 AI 内容生成系统，通过 Prompt 工程优化生成质量，实现流式输出提升用户体验

2. **全栈开发能力**
   > 基于 Next.js 15 App Router 实现前后端一体化架构，使用 Supabase 作为 BaaS 方案，支持多用户数据隔离

3. **现代化技术栈**
   > 采用 TypeScript + Zustand + shadcn/ui 构建类型安全的前端应用，使用 React Hook Form 实现高性能表单管理

### 可量化数据（面试时怎么说）
- "支持 3 种 AI 能力（内容优化/技能推荐/自我评价生成）"
- "实现了基于 Row Level Security 的多租户数据隔离"
- "使用 JWT + httpOnly Cookie 保证认证安全"
- "支持实时预览和 PDF 导出，提供 2 种简历模板"

### 可深挖的技术点（面试官可能追问）
1. **为什么选择 Zustand 而不是 Redux？**
   - Zustand 更轻量，API 更简洁，适合中等复杂度项目
   - 不需要 Provider 包裹，减少组件嵌套

2. **如何保证 API Key 安全？**
   - API Key 存储在服务器环境变量
   - 前端通过 Next.js API Route 调用 AI
   - 使用 httpOnly Cookie 存储 JWT Token

3. **如何实现 AI 流式输出？**
   - 使用 ReadableStream API
   - 前端使用 `fetch` 的 `response.body.getReader()`
   - 逐块解析数据并更新 UI

4. **如何实现多用户数据隔离？**
   - 使用 Supabase Row Level Security
   - 在 JWT Token 中存储 userId
   - 后端中间件自动验证并注入用户上下文

---

## ✅ 项目验收清单

### 功能验收
- [ ] 可以用 3 个演示账号登录
- [ ] 可以创建/编辑/删除简历
- [ ] AI 内容优化功能正常工作
- [ ] 可以切换简历模板并预览
- [ ] 可以导出 PDF 文件
- [ ] 不同用户的数据完全隔离

### 代码质量
- [ ] 无 TypeScript 类型错误
- [ ] 无 ESLint 警告
- [ ] 关键功能有注释
- [ ] API 有错误处理

### 文档完整性
- [ ] README 包含项目介绍和运行指南
- [ ] 环境变量有示例文件（`.env.example`）
- [ ] 有清晰的 Git 提交历史

### 部署检查
- [ ] 生产环境部署成功
- [ ] 所有环境变量配置正确
- [ ] 数据库连接正常
- [ ] AI API 调用正常

---

## 📞 沟通约定

### AI 开发助手的工作模式
1. **每次只聚焦 1-2 个问题**
2. **决策前先询问，不擅自修改技术方案**
3. **遇到歧义先澄清，不做假设**
4. **阶段性总结变更内容**

### 用户的响应方式
- 明确说"同意"或"不同意"
- 对不理解的技术点及时提问
- 发现问题立即反馈

---

**文档结束**

> 最后更新：2026-09-11  
> 如有疑问或需要调整，请随时沟通。
